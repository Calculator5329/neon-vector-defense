name: Firebase Deploy

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/firebase-deploy.yml'

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Firebase deploy workflow is valid. Manual deploy steps are intentionally disabled while CI is being repaired."
