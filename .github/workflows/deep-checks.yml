name: Deep Checks

on:
  workflow_dispatch:
    inputs:
      suite:
        description: Deep check suite
        required: true
        type: choice
        options:
          - quick-balance
          - full-balance
          - browser-perf
          - all
        default: quick-balance

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ inputs.suite }}
  cancel-in-progress: true

jobs:
  deep:
    name: ${{ inputs.suite }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm
      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21
      - name: Install Dependencies
        run: npm ci
      - name: Install Browser
        if: ${{ inputs.suite == 'browser-perf' || inputs.suite == 'all' }}
        run: npx playwright install --with-deps chromium
      - name: Quick Balance Simulation
        if: ${{ inputs.suite == 'quick-balance' || inputs.suite == 'all' }}
        run: npm run sim -- quick
      - name: Full Balance Simulation
        if: ${{ inputs.suite == 'full-balance' || inputs.suite == 'all' }}
        run: npm run sim
      - name: Browser Performance Harness
        if: ${{ inputs.suite == 'browser-perf' || inputs.suite == 'all' }}
        run: npm run build && npm run perf:browser
      - name: Upload deep-check artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: deep-check-artifacts-${{ inputs.suite }}
          path: |
            playwright-report/
            test-results/
            public/balance-report.json
            public/tower-deep-dive-report.json
            public/tower-deep-dive-sims.csv
          if-no-files-found: ignore
