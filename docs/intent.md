# Product intent

## Anti-slop cleanup — Ethan, 2026-09-04

The current explicit request authorizes behavior-preserving code cleanup in this
named project for this work block, despite the older standing docs-only fence.
It does not promote the standing fence for unrelated future work. Preserve
gameplay and score verification. Remove proven unused implementation, retain
useful tests, and present working-feature retirement candidates separately.

## Menu repair review — 2026-09-04

When moving the sole caller of a CSS rule, replace its obsolete positioning
in that rule instead of adding an override that leaves unused offsets behind.
The YAKKOB unlock must remain visible and clickable on narrow portrait screens;
moving it into a rail hidden at that breakpoint is a regression. Keep the
existing hidden rail behavior on Operations and Leaderboard screens.

Narrow protocol cards must keep short names such as Recruit intact. Stack
the name and description when the dock cannot fit both comfortably; preserve
the recommendation pill above them. Review focused snapshots for readable
content before accepting a baseline.
